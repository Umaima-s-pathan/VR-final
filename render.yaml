services:
  - type: web
    name: palace-vr180-backend
    env: node
    buildCommand: npm install  # Install at root level
    startCommand: cd server && npm start  # Run from server directory
    port: 3001
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
