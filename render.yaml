services:
  - type: web
    name: palace-vr180-backend
    env: node
    buildCommand: npm install
    startCommand: node server/index.js
    port: 3001
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
